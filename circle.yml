machine:
  node:
    version: '6'

test:
  override:
    - make verify
    - make test
    - npm run whitesource

deployment:
  release:
    branch: master
    commands:
      - make deploy
      - make change-request
      - make update-cmdb
